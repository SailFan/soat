<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.tool.soat.dao.SoatPermissionMapper" >
  <resultMap id="BaseResultMap" type="com.tool.soat.entity.SoatPermission" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    <result column="pid" property="pid" jdbcType="INTEGER" />
    <result column="code" property="code" jdbcType="VARCHAR" />
    <result column="name" property="name" jdbcType="VARCHAR" />
  </resultMap>


  <select id="queryPermissionByUsername" resultSets="java.util.Set" resultType="string">
    SELECT soat_permission.`code`
    FROM soat_users
    INNER JOIN soat_users_roles
    ON soat_users.id = soat_users_roles.u_id
    INNER JOIN soat_roles
    ON soat_users_roles.r_id=soat_roles.rid
    INNER JOIN soat_roles_permissions
    on soat_roles.rid=soat_roles_permissions.r_id
    INNER JOIN soat_permission
    ON soat_roles_permissions.p_id =soat_permission.pid
    WHERE nickname=#{nickname}
  </select>

  <insert id="insert" parameterType="com.tool.soat.entity.SoatPermission" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into soat_permission (pid, code, name
      )
    values (#{pid,jdbcType=INTEGER}, #{code,jdbcType=VARCHAR}, #{name,jdbcType=VARCHAR}
      )
  </insert>
  <insert id="insertSelective" parameterType="com.tool.soat.entity.SoatPermission" >
    <!--
      WARNING - @mbggenerated
      This element is automatically generated by MyBatis Generator, do not modify.
    -->
    insert into soat_permission
    <trim prefix="(" suffix=")" suffixOverrides="," >
      <if test="id != null" >
        pid,
      </if>
      <if test="code != null" >
        code,
      </if>
      <if test="name != null" >
        name,
      </if>
    </trim>
    <trim prefix="values (" suffix=")" suffixOverrides="," >
      <if test="pid != null" >
        #{id,jdbcType=INTEGER},
      </if>
      <if test="code != null" >
        #{code,jdbcType=VARCHAR},
      </if>
      <if test="name != null" >
        #{name,jdbcType=VARCHAR},
      </if>
    </trim>
  </insert>
</mapper>